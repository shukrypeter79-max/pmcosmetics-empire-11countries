PORT=3000
SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY is a server-side secret. DO NOT commit, expose, or use it in client-side code.
# For local testing, copy .env.example to .env and use a staging service key (never a production key).
SUPABASE_SERVICE_ROLE_KEY=

# Shopify Skeleton Adapter

Mock-only adapter implementing the Commerce Hub channel contract.

No Shopify credentials or network calls are included. Replace the mock operations with authenticated channel calls only in a later integration PR.
name: Deno

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-template:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled unrelated template
        run: echo "Deno template disabled; PM Cosmetics Hub uses Node.js."
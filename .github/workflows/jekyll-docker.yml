name: Jekyll site CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-template:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled unrelated template
        run: echo "Jekyll template disabled; this repository is not the Jekyll deployment target."
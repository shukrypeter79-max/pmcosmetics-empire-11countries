NodeJS with Webpack

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-template:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled unrelated template
        run: echo "NodeJS Webpack template disabled; PM Cosmetics Hub CI is the canonical runtime check."
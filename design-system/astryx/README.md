# Astryx Design System

Temporary placeholder. The verified Astryx fork URL has not yet been supplied, so no external code is copied here and no UI dependency is enabled.

When the source URL is provided, import its history into this path without opening the catalog Gate.
